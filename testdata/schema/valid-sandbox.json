{
  "sandbox": {
    "enabled": true,
    "network": false,
    "image": "alpine:3.20"
  },
  "tests": [
    {
      "desc": "runs with no network in a pinned image",
      "cmd": "echo produced > out.txt"
    }
  ]
}
